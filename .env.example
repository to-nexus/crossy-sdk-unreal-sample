# ============================================================================
#  Local environment template for CrossySdkUnrealSamp.
#
#  This file is OPTIONAL. The plugin install scripts work out of the box
#  against the public registry repo (`to-nexus/crossy-sdk-unreal-sample`)
#  without any authentication.
#
#  Copy to `.env` and fill in only if you actually need it. `.env` is
#  .gitignore'd and is auto-loaded by the Makefile.
#
#    cp .env.example .env
#    # edit .env if needed
#    make sdk-install
# ============================================================================

# GITHUB_TOKEN — OPTIONAL.
#
# When unset, install-plugins.{sh,ps1} downloads public GitHub release assets
# directly and does not use the GitHub REST API.
#
# Set this only if you switch the registry to a private repo (advanced, not the
# default).
#
# Public repos require no token. Private repos need a PAT with repo access.
#GITHUB_TOKEN=

