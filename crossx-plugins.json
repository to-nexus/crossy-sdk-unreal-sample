{
  "$schema": "https://crossnexus.com/schemas/crossx-plugins.schema.json",
  "comment": "Edit the versions below, then run `make sdk-install`. Registry repo is public, so no GITHUB_TOKEN is required (an optional token only raises GitHub's anonymous rate limit).",
  "registry": {
    "type": "github-releases",
    "owner": "to-nexus",
    "repo": "crossy-sdk-unreal-sample"
  },
  "plugins": {
    "CROSSxSdkUnrealPlugin": "0.0.0-beta.16"
  }
}
